---
interface Props {
	readonly href: string;
}

const { href } = Astro.props;
const isCurrent = href === Astro.url.pathname;
---

<style>
	.current {
		@apply scale-105 -rotate-2 font-semibold;
	}

	a:not(.current) {
		@apply hover:scale-105 hover:-rotate-2 hover:text-black hover:underline;
	}
</style>

<li>
	<a href={href} class:list={["inline-block", { current: isCurrent }]}>
		<slot />
	</a>
</li>
